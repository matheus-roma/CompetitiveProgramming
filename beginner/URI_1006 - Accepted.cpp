#include <iostream>
 
using namespace std;
 
int main() {
 
	double X, A, B, C;
	cin >> A;
	cin >> B;
	cin >> B;
	X = ((A*2.0) + (B*3.0) + (C*5.0))/10; 
	cout << "MEDIA = " << X << endl;
  return 0;
}

// apenas uma casa decimal
